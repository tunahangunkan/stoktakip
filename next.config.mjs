/** @type {import('next').NextConfig} */
const nextConfig = {
  // İkas panelinin bu uygulamayı iframe içinde açabilmesi için
  // X-Frame-Options / CSP ayarları ileride panel eklerken yapılandırılacak.
};
export default nextConfig;
